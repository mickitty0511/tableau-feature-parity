# Constraints

## PPTX生成
- 主要テキストは画像化せず、PowerPoint上で編集可能な要素として保持する。
- 中間生成物は `tmp/` 配下に置く。
- 最終成果物は `docs/tech-posts/post/` 配下に置く。

## セキュリティと環境ファイル
- このタスクでは秘密情報や環境変数を扱わない。
- `.env`、秘密鍵、トークンファイルはリポジトリ内に作成しない。

## 検証
- 資料生成タスクのため、単体テストとE2Eテストは対象外。
- 代わりに PPTXレンダリング、編集性検査、pre-check を完了条件にする。
